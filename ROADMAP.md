# 規劃中的模組（未隨本包提供）

本包提供依賴鏈上的**地基層與戰術層**六個技能（見 [`README.md`](README.md)）。**策略層的三個模組這次沒有提供**，這份檔案說明它們是什麼、為什麼分開、以及沒有它們時本包的行為。

**這是位置說明，不是技能內容。** 這三個目錄不存在於本包，這份檔案也不包含它們的方法論——想知道它們會怎麼運作，看下面的「負責什麼」欄就是目前公開的全部資訊。

## 三個模組

| 模組 | 在依賴鏈的位置 | 負責什麼 | 本包哪些技能會提到它 |
|---|---|---|---|
| **`usp-discovery`** | 第 2 層（切角設計的上游） | 挖掘價值主張——這個產品憑什麼贏、對誰而言、相對於誰 | `ads`、`ad-creative`、`landing-page-cro` 的「開始前」切角文件檢查；`landing-page-cro` 的 LIFT 中心因子 |
| **`campaign-strategy`** | 第 3 層（戰術層的直接上游） | 切角與活動策略設計——這一檔要打誰、用什麼角度、對比誰 | 同上；`landing-page-cro` 訊息一致性三方比對的第一方 |
| **`strategy-recalibration`** | 第 6 層（成效分析的下游） | 判斷切角／USP 假設本身是否成立，決定要不要繞回第 2／3 層重新設計 | `campaign-analysis-iteration` 判斷結果指向「不是素材也不是投放的問題」時 |

## 為什麼 `strategy-recalibration` 不是「再迭代一次素材」

這一層最容易被誤解，所以講清楚：`campaign-analysis-iteration`（本包已含）回答的是「**這支廣告／這個素材**該不該繼續」，它的所有結論都在**執行層**——換素材、改受眾、調預算、修到達頁。

`strategy-recalibration` 回答的是完全不同的問題：「**這個切角本身**是不是打不動？」換了五輪素材、修過到達頁、投遞診斷也乾淨，成效還是不行的時候，問題可能不在任何一個執行環節，而在最上游的假設——鎖定的痛點根本不是這群人的主要痛點、或差異化主張對他們沒有意義。

**這兩件事的處置方向相反**：前者是「在同一個切角底下換一個執行版本」，後者是「回頭改切角，前面所有執行都要重做」。把後者誤當成前者，就會出現「一直換素材、每次都小幅改善又打回原形」的循環。所以 `campaign-analysis-iteration` 在所有執行層原因都排除之後，會**如實告知「這可能是切角層的問題」並停在那裡，不越級判斷切角要不要換**。

## 沒有這三個模組時，本包怎麼運作

**六個技能仍然獨立可用。** 三個戰術技能（`ads`／`ad-creative`／`landing-page-cro`）在開始前會檢查專案內有沒有切角／價值主張文件——常見位置如 `.agents/positioning.md`、`.agents/campaign-strategy.md`、`.agents/usp.md`、`strategy/` 或 `docs/` 底下的相關文件：

| 情況 | 行為 |
|---|---|
| **找到切角文件** | 讀過再開始，以它為基準，交付時指出依據哪一份、哪一段 |
| **沒找到** | **照樣執行**，但據實告知這是在沒有策略基礎的前提下給的通用建議，並說明少了哪一層判斷 |

**這是軟性提示，不是硬擋**——技能不會拒絕工作，也不會假裝有那份文件。

各技能少了切角文件時具體差在哪：

- **`ads`**：受眾與平台的選擇是按產業常態推的，不是從你的差異化主張推的
- **`ad-creative`**：模式一的「定義角度」退回自己定義，這批素材會同時在測「角度對不對」跟「表達方式好不好」兩件事，結論比較難歸因
- **`landing-page-cro`**：訊息一致性檢查從**三方比對降級成兩方比對**（到達頁 vs 廣告文案）——查得出「廣告講 A、頁面講 B」，查不出「兩邊講的是同一件事，但那件事本來就不是你要打的主張」

**你也可以自己提供切角文件**，不一定要等這些模組。內容只要能回答三件事就足以讓上述技能用起來：**要打誰、憑什麼贏、對比誰**。放在專案內上述任一位置即可。

不想自己寫的話，本包的 **`quick-angle`** 就是問這三題並整理成 `.agents/positioning.md` 的技能。它跟上表的 `usp-discovery`／`campaign-strategy` 涵蓋範圍不同：

| | `quick-angle`（本包已含） | `usp-discovery`／`campaign-strategy`（未提供） |
|---|---|---|
| 輸入 | 使用者對三個問題的回答 | 客戶語言、競品拆解、市場資料 |
| 對答案的處理 | 如實整理，不驗證是否成立 | 挖掘、比對、檢驗假設 |
| 候選數量 | 一個 | 多個候選的設計與取捨 |
| 產出 | 10-20 行的切角文件 | 價值主張論述、活動策略 |

也就是說，`quick-angle` 只負責把答案落成下游讀得懂的檔案，答案本身對不對它不判斷，產出的文件會寫明這一點。

## 狀態

規劃中，尚未排定釋出時間。本檔案會隨規劃調整更新。
