# 依賴鏈地圖與技能邊界

本包的技能邊界按**行銷知識由上而下的依賴關係**畫，不是按「工具功能」分（總覽見 [`README.md`](README.md)）。這份檔案說明六個技能各自站在依賴鏈的哪一層、彼此怎麼交接，以及每支技能明確不做什麼。

## 六個技能在依賴鏈的位置

| 技能 | 在依賴鏈的位置 | 負責什麼 | 交接給誰／從誰接手 |
|---|---|---|---|
| **`tracking-health`** | 橫向地基 | 建立、健檢、除錯追蹤與量測；個資外洩防護；歸因限制 | 所有層的判斷都建立在這裡的數字可不可信；成效數字可疑時各技能都會退回這裡 |
| **`quick-angle`** | 切角層（戰術層的上游） | 問三題（要打誰、憑什麼贏、對比誰），整理成 10-20 行的切角文件 `.agents/positioning.md` | 產出給三個戰術技能的「開始前」檢查讀取 |
| **`ads`** | 戰術層 | 選平台、搭帳戶結構、配預算、設受眾、過台灣廣告合規 | 讀切角文件；成效判斷交給 `campaign-analysis-iteration`、素材交給 `ad-creative` |
| **`ad-creative`** | 戰術層 | 定角度、寫文案、出靜圖概念、規劃素材產能 | 讀切角文件；受規管產業強制載入 `ads` 的合規檔；贏輸判斷交給 `campaign-analysis-iteration` |
| **`landing-page-cro`** | 戰術層 | 用 LIFT 六因子診斷到達頁；訊息一致性檢查 | 讀切角文件與廣告文案做三方比對；統計判定不做 |
| **`campaign-analysis-iteration`** | 成效層（戰術層的下游） | 從資料判斷該不該繼續——觸發關閉門檻／留／放大、素材該不該換 | 結論回頭指向 `ad-creative`／`ads`／`landing-page-cro`／`tracking-health` |

**每支技能都是「輕量路由入口（`SKILL.md`）＋主題化 `references/`」的漸進揭露結構**：`SKILL.md` 只放角色定義、範圍邊界、硬性規則與路由表，實際的門檻、清單、範本在 `references/` 裡按需載入。

## 切角文件是戰術層的共同輸入

三個戰術技能（`ads`／`ad-creative`／`landing-page-cro`）在開始前都會檢查專案內有沒有切角／價值主張文件——常見位置如 `.agents/positioning.md`、`.agents/usp.md`、`.agents/` 底下其他切角／價值主張／活動策略文件，或 `strategy/`、`docs/` 底下的相關文件：

| 情況 | 行為 |
|---|---|
| **找到切角文件** | 讀過再開始，以它為基準，交付時指出依據哪一份、哪一段 |
| **沒找到** | **照樣執行**，但據實告知這是在沒有策略基礎的前提下給的通用建議，並說明少了哪一層判斷 |

**這是軟性提示，不是硬擋**——技能不會拒絕工作，也不會假裝有那份文件。

各技能少了切角文件時具體差在哪：

- **`ads`**：受眾與平台的選擇是按產業常態推的，不是從你的差異化主張推的
- **`ad-creative`**：模式一的「定義角度」退回自己定義，這批素材會同時在測「角度對不對」跟「表達方式好不好」兩件事，結論比較難歸因
- **`landing-page-cro`**：訊息一致性檢查從**三方比對降級成兩方比對**（到達頁 vs 廣告文案）——查得出「廣告講 A、頁面講 B」，查不出「兩邊講的是同一件事，但那件事本來就不是你要打的主張」

**切角文件可以自己寫**，內容只要能回答三件事就足以讓上述技能用起來：**要打誰、憑什麼贏、對比誰**。放在專案內上述任一位置即可。不想自己寫的話，本包的 **`quick-angle`** 就是問這三題並整理成 `.agents/positioning.md` 的技能。

`quick-angle` 的範圍界線寫得很死：它只負責**把答案落成下游讀得懂的檔案**，不做市場研究、不驗證那個答案站不站得住腳、不產出多個候選切角讓你取捨。答案本身對不對它不判斷，產出的文件會在「限制」段寫明這一點。

## 「切角層的問題」不等於「再迭代一次素材」

這個區分是本包最容易被誤解的地方，所以講清楚：`campaign-analysis-iteration` 回答的是「**這支廣告／這個素材**該不該繼續」，它的所有結論都在**執行層**——換素材、改受眾、調預算、修到達頁。

但還有一個完全不同的問題：「**這個切角本身**是不是打不動？」換了五輪素材、修過到達頁、投遞診斷也乾淨，成效還是不行的時候，問題可能不在任何一個執行環節，而在最上游的假設——鎖定的痛點根本不是這群人的主要痛點、或差異化主張對他們沒有意義。

**這兩件事的處置方向相反**：前者是「在同一個切角底下換一個執行版本」，後者是「回頭改切角，前面所有執行都要重做」。把後者誤當成前者，就會出現「一直換素材、每次都小幅改善又打回原形」的循環。

**本包的技能不做後者的判斷。** 所以 `campaign-analysis-iteration` 在所有執行層原因都排除之後，會**如實告知「這可能是切角層的問題」並停在那裡，不越級判斷切角要不要換**——這是刻意的邊界，不是缺漏：技能該誠實承認自己判斷不到的地方，而不是硬給一個沒有依據的結論。

## 未隨本包提供的第三方技能

各技能路由表裡出現、但不屬於本包的技能（需要你的 agent 環境另外安裝）：

- **`ab-testing`**：受控實驗的設計與統計判定——`landing-page-cro` 與 `campaign-analysis-iteration` 都不做統計判定
- **`copywriting`**：廣告流量導向與到達頁的長文案撰寫
